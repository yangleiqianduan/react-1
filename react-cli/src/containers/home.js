import React, { Component } from 'react';
import ReactDOM from 'react-dom';

class Home extends Component {
    render() {
        return (
            <div>Home</div>
        );
    }
}

//const Home = ReactDOM.render(<HomeComponent/>, document.getElementById('root'));

export default Home;