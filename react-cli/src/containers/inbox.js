import React, { Component } from 'react';
import ReactDOM from 'react-dom';

class Inbox extends Component {
    render() {
        return (
            <div>Inbox</div>
        );
    }
}

//const Inbox = ReactDOM.render(<InboxComponent/>, document.getElementById('root'));

export default Inbox;